name: Test invalid replace_mode error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a dummy target file
        run: echo "hello world" > /tmp/test-upload.txt

      - name: Build a valid-looking (but fake) credentials JSON and base64-encode it
        id: creds
        run: |
          CREDS_JSON='{"client_email":"test@test-project.iam.gserviceaccount.com","private_key":"-----BEGIN RSA PRIVATE KEY-----\nMIIEowIBAAKCAQEA0Z3VS5JJcds3xHn/ygWep4PAtEsHAFOCkMBFCFMBFCFMBFCF\n-----END RSA PRIVATE KEY-----\n","type":"service_account"}'
          ENCODED=$(echo -n "$CREDS_JSON" | base64 -w 0)
          echo "encoded=$ENCODED" >> $GITHUB_OUTPUT

      - name: Run action with invalid replace_mode (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          credentials: ${{ steps.creds.outputs.encoded }}
          parent_folder_id: "fake-folder-id"
          target: "/tmp/test-upload.txt"
          replace_mode: "not_a_valid_mode"

      - name: Assert action failed due to invalid replace_mode
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid replace_mode, but outcome was '${{ steps.action.outcome }}'"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid replace_mode"
